import { Element } from 'react-scroll';
import heroImage from '/hero.png'

const Hero = () => {
    return (
        <Element name="Home">
            <section className="relative h-screen overflow-hidden w-full bg-primary text-white">

            </section>
        </Element>
    );
};

export default Hero;

